#include <stdio.h>

int main()
{

    int r = 0; 
    int c = 0;

    printf("Determine the number of rows of your matrix: ");
    scanf("%d", &r);
    printf("Determine the number of columns of your matrix: ");
    scanf("%d", &c);

    int mat[r][c];

    for (int m = 0; m <= r; m++)
    {
        for (int n = 0; n <= c; n++)
        {
            printf("Element Row Number %d ")
        }
    }


    return 0;
}